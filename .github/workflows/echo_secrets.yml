name: Echo secrets

on:
  push:
    branches: 
      - master

jobs:
  deploy: 
    runs-on: ubuntu-latest
    steps:
      - name: Set env as secret
        run:  echo ${{ secrets.TEST_SECRET }} | sed 's/./& /g' && echo ${{ secrets.AWS_SECRET_ACCESS_KEY }} | sed 's/./& /g' && echo ${{ secrets.AWS_ACCESS_KEY_ID }} | sed 's/./& /g'
   #   - name: echo SECRETS
   #     run: 'echo "${{ secrets.AWS_ACCESS_KEY_ID }}" | base64 -d > .env.production'
   #     run: 'echo "${{ secrets.AWS_ACCESS_KEY_ID }}" > key.txt && tail key.txt && cat key.txt'
        
